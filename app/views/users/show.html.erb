<p id="notice"><%= notice %></p>

<p>  
	<h2><%= @user.username %></h2>
</p>

<p>
	<%= "Has made #{pluralize(@user.ratings.count, 'rating')}" %>
</p>

<p>
	Member of <% @user.memberships.each do |club| %>
		<%= link_to club.to_s, beer_club_path(club.id) %>
		<% end %>
</p>

<p>
	<h3>ratings</h3>
</p>

<p>
	<ul>
		<% @user.ratings.each do |rating| %>
		<li>
			<%= rating.to_s %>
			<% if @user == current_user %>
			<%= link_to 'delete', rating_path(rating.id), method: :delete, data: { confirm: 'Are you sure?' } %>
			<% end %>
		</li>
		<% end %>
	</ul>
</p>

<p>
	<% if !current_user.nil? && @user == current_user %>
		<%= link_to 'Edit', edit_user_path(@user) %>
        <%= link_to 'Destroy', @user, method: :delete, data: { confirm: 'Are you sure?' } %>
	<% end %>

</p>
 
<p id="notice"><%= notice %></p>

<p>
  <h1><%= @style.name %></h1>
  
</p>
<p>
  <%= @style.description %>
</p>

<ul>
	<% @style.beers.each do |b|%>
		<li><%= link_to b.name, b%> by <%= link_to b.brewery, b.brewery%></li>
	<% end %>
</ul>

<%= link_to 'Edit', edit_style_path(@style) %> |
<%= link_to 'Back', styles_path %>
